package com.xyz.bu.handler.encrypt;

/**
 * 加密/解密pojo
 * <p>
 * 适用于封装请求body的pojo用作解密以及封装作为返回值的pojo用于加密
 * <p>
 * 必须配套 {@link EncryptField} 使用，否则不起作用
 * <p>
 * 具体如何处理详见 {@link EncryptHandler}
 *
 * @author xyz
 * @date 2021/10/25
 **/
public interface EncryptPojo {
}
